<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ item.title }}</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <h1>{{ item.title }}</h1>
    <p>Served by {{ ansible_hostname }}</p>
  </header>
  <main>
    <p>This is the <strong>{{ item.slug }}</strong> application, served behind the
       High-Availability Nginx Platform's ALB and reverse proxy.</p>
  </main>
</body>
</html>
