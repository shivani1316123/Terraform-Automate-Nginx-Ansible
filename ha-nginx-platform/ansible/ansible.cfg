[defaults]
inventory       = inventory.ini
host_key_checking = False
remote_user     = ubuntu
private_key_file = ~/.ssh/ha-nginx-key.pem
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
